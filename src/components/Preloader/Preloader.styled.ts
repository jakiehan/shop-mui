import { CircularProgress, Box, styled } from '@mui/material';

const PreloaderStyled = styled(Box)(({ theme }) => ({
  margin: '9.4rem auto 0',
  display: 'flex',
  justifyContent: 'center',
}));

export { CircularProgress, PreloaderStyled }

